PORT=5000
NODE_ENV=production

# Security
# Generate a new secure random string for production
JWT_SECRET=dispatchng_super_secret_jwt_key_2026_production_safe
JWT_EXPIRES_IN=7d

# Frontend URL (Used for CORS in development, but not strictly needed if served statically)
CLIENT_URL=http://localhost:5173
